package ds2022

object Exo1 {


  //Question 1
  /*
  val q1 = () => 2 // Unit => Int
  val q2 = (f, g) => f(true) && g(2) //(Boolean => Boolean, Int => Boolean) => Boolean
  val q3 = (l, f, p) => (l map f) filter p // (List[T], T => A, A => Boolean) => List[A]
  val q4 = (l, p) => (l map p) filter p // (List[Boolean], Boolean => Boolean) => List[Boolean]
  val q5 = l => (l foldLeft 0)((acc, x) => acc-x) //List[Int] => Int

  //Question 2
  val v1 = (x => x + 1)((() => 2)()) // 3
  val v2 = (x => if (x) 3 else println(x); 0)(3 > 1) // 3
  val v3 = List(1, -2, 3) map (x => x > 0) // List(true, false, true)
  val v4 = List(-1, 2, -3) filter (x => x > 0) // List(2)
  val v5 = (List(-1, 2, 3) foldLeft true) ((acc, x) => acc && x > 0) // false

   */

}
