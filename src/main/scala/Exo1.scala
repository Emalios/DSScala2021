object Exo1 {

  /*
  Question 1 (2,5 points) – Donner un type aux fonctions suivantes. Utilisez un type polymorphe si possible.
  — x => true                 T => Boolean
  — x => println(x)           T => Unit
  — x => if (x) println(x)    Boolean => Unit
  — (f, g) => f(true)+g(2)+3  (Boolean => Number, Int => Number) => Number
  — l => l map (x => x)       List[T] => List[T]


Question 2 (2,5 points) – Quel est le résultat des appels de fonction suivants ? On rappelle que % est le modulo.
  — (x => true)(3)                                  true
  — (x => if (x) println(x))(false)
  — List(1, 2, 3) map (x => x % 2 == 0)             List(false, true, false)
  — List(1, 2, 3) filter (x => x % 2 == 0)          List(2)
  — (List(3, 2, 1) foldLeft 8)((acc, x) => acc % x) 1
*/

}
